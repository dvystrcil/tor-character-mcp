apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tor-character-mcp
resources:
  - namespace.yaml
  - harbor-pull-secret.yaml
  - deployment.yaml
  - service.yaml
  - image-updater-rbac.yaml
  - vpa.yaml
images:
  - name: harbor.sirddail.net/ai/tor-character-mcp
    newTag: 0.0.5
