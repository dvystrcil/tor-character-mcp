apiVersion: v1
kind: Namespace
metadata:
  name: tor-character-mcp
  labels:
    app.kubernetes.io/part-of: homelab-mcp
    # gateway-services chart needs to route from istio-system; label
    # here so the chart's multi-source HTTPRoute selector matches.
    istio.io/dataplane-mode: ambient
