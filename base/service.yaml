apiVersion: v1
kind: Service
metadata:
  name: tor-character-mcp
  namespace: tor-character-mcp
  labels:
    app.kubernetes.io/name: tor-character-mcp
    app.kubernetes.io/part-of: homelab-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tor-character-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
